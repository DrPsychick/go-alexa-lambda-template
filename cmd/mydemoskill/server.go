package main

import (
	"github.com/urfave/cli/v2"
)

func runServer(c *cli.Context) error {
	// TODO!
	// s := rpc.NewServer(app)
	// logger.Info(ctx, "Starting lambda server")
	// if err :=

	return nil
}
